<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.vitinew">

    <uses-sdk tools:overrideLibrary="com.truecaller.android.sdk" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".PaymentViaAmazonGiftCard"></activity>
        <activity android:name=".PaymentViaPaytm" />
        <activity android:name=".AboutCampaign" />
        <activity android:name=".question_answer" />
        <activity android:name=".ProjectDetail" />
        <activity android:name=".gigDetails" />
        <activity
            android:name=".LoginRegister"
            android:theme="@style/LoginRegister" />
        <activity
            android:name=".Login_activity"
            android:theme="@style/LoginTheme" />
        <activity android:name=".ui.Extras.BaseActivity" />
        <activity
            android:name=".Register"
            android:theme="@style/LoginTheme" />
        <activity
            android:name=".MainActivity"
            android:label="Viti"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.truecaller.android.sdk.PartnerKey"
            android:value="@string/partnerKey" />
    </application>

</manifest>